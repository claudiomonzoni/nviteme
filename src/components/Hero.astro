---
interface Props {
	title: string;
	body: string;
	href: string;
}

const { href, title, body } = Astro.props;
---

<section id="hero" class="grid contenido">
	<h1>Nombre de la novia & Nombre del novio</h1>
</section>